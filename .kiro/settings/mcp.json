{
  "mcpServers": {
    "context-api": {
      "type": "http",
      "url": "http://localhost:8001",
      "description": "Context Wiki API — routed via auth proxy which handles token refresh automatically",
      "openApiSpec": "http://localhost:8001/openapi.json"
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres"],
      "env": {
        "POSTGRES_CONNECTION_STRING": "${DATABASE_URL}"
      },
      "description": "Direct PostgreSQL access for schema inspection and debugging"
    }
  }
}
