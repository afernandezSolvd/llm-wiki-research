{
  "mcpServers": {
    "context-wiki": {
      "command": "python",
      "args": ["-m", "app.mcp.server"],
      "description": "Context Wiki API — ingest, query, lint, wiki pages, knowledge graph"
    }
  }
}
